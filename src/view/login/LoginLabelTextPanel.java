package view.login;

import javax.swing.*;

public class LoginLabelTextPanel extends JPanel{
    LoginLabelTextPanel(JLabel label, JTextField textField) {
        this.add(label);
        this.add(textField);
    }
}
