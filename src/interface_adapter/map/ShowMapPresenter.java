package interface_adapter.map;

import use_case.map.ShowMapOutputBoundary;
import use_case.map.ShowMapOutputData;

public class ShowMapPresenter implements ShowMapOutputBoundary {

    @Override
    public void prepareSuccessfulView(ShowMapOutputData showMapOutputData) {

    }
}
